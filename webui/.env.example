# Copy to .env.local (gitignored by the repo root's .env.* rule) for
# `npm run dev` against real hardware.
# Your device's current mDNS name or IP -- check the serial monitor at
# boot, or GET /api/v1/wifi once it's already reachable some other way.
VITE_DEVICE_HOST=glide.local
